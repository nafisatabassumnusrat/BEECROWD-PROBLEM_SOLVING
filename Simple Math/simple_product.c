#include <stdio.h>

int main() {
    int A, B, PROD;
    scanf("%d %d", &A, &B);  // দুইটি পূর্ণসংখ্যা ইনপুট নেওয়া
    PROD = A * B;            // গুণফল বের করা
    printf("PROD = %d\n", PROD);  // আউটপুট নির্দিষ্ট ফরম্যাটে
    return 0;
}
